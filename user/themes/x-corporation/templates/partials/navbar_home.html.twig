{% spaceless %}
{% macro loop(page) %}
    {% for p in page.children.visible %}
        {% set current_page = (p.active or p.activeChild) ? 'active' : '' %}
        {% if p.children.visible.count > 0 %}
            <li class="dropdown m-menu-fw {{ current_page }}">
                <a href="#" data-toggle="dropdown" class="dropdown-toggle">{{ p.menu }}</a>
                    {% if p.children.visible.count > 0 %}
                    <ul class="dropdown-menu">
                    {% for dropdownHeader in p.children.visible %}
                        <li class="m-menu-content">
                           <a href="{{ dropdownHeader.url }}">{{ dropdownHeader.menu }}</a>
                        </li>
                    {% endfor %}
                    </ul>
                    {% endif %}
            </li>
        {% else %}
            <li class="{{ current_page }}">
                <a href="{{ p.url }}">{{ p.menu }}</a>
            </li>
        {% endif %}
    {% endfor %}
{% endmacro %}
{% endspaceless %}

<nav class="navbar m-menu navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <ul class="nav-cta visible-xs">
                <li class="dropdown"><a href="#" data-toggle="dropdown" class="dropdown-toggle" title="tìm kiếm"><i
                        class="fa fa-search"></i></a>
                    <ul class="dropdown-menu">
                        <li>
                            {% include 'partials/simplesearch_searchbox.html.twig' %}
                        </li>
                    </ul>
                </li>
            </ul>
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
             {% if config.plugins.simplesearch.enabled %}
            
            {% endif %}
            {% if logo != '' %}
            <h1><a class="navbar-brand" href="{{ base_url == '' ? '/' : base_url }}" title="menu">
                <img src="{{ url('theme://img/' ~ logo) }}" alt="menu">
            </a><h1>
            
            {% endif %}
        </div>
        <ul class="nav-cta visible-sm visible-md visible-lg ">
            <li class="dropdown"><a href="#" data-toggle="dropdown" class="dropdown-toggle" title="tìm kiếm"><i
                    class="fa fa-search"></i></a>
                <ul class="dropdown-menu">
                    <li>
                        {% include 'partials/simplesearch_searchbox.html.twig' %}
                    </li>
                </ul>
            </li>
        </ul>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right main-nav">
                {{ _self.loop(pages) }}
            </ul>

        </div>
    </div>
</nav>
